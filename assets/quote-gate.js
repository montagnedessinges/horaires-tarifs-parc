(function ($) {
    'use strict';
    var config = window.ParcsHTQuoteGate || null;
    if (!config) return;
    var visitField=String(config.visitField||'visite'), groupField=String(config.groupField||'groupedevis');
    function contactHtml(value) { value=String(value||'').trim(); if(!value)return ''; var href=value.indexOf('@')>0&&value.indexOf('://')<0?'mailto:'+value:value; return ' <a href="'+$('<div>').text(href).html()+'">'+$('<div>').text(value).html()+'</a>'; }
    function fieldSelector(name){return '[name="'+String(name).replace(/"/g,'\\"')+'"]';}
    function parts($form) { var $date=$form.find(fieldSelector(visitField)).first(), $datePart=$date.closest('.cp'), $container=$datePart.parent(); return {date:$date,datePart:$datePart,container:$container,others:$container.children().not($datePart).not('.parcs-ht-quote-gate-message').not('input[type="hidden"]')}; }
    function box($form) { var p=parts($form),$box=p.container.children('.parcs-ht-quote-gate-message'); if(!$box.length){$box=$('<div class="parcs-ht-quote-gate-message cp full" role="status" aria-live="polite"></div>');p.datePart.after($box);} return $box; }
    function showMessage($form,text,contact,kind){var $box=box($form);if(!text){$box.empty().hide();return;}$box.attr('data-kind',kind||'').html($('<div>').text(text).html()+contactHtml(contact)).show();}
    function init($form){if(!$form.length||$form.data('parcsHtGateReady'))return;if(!$form.closest('.parcs-ht-quote').length)return;if(!$form.find(fieldSelector(visitField)).length||!$form.find(fieldSelector(groupField)).length)return;$form.data('parcsHtGateReady',true);var p=parts($form);if(!p.datePart.length)return;p.others.hide();box($form).hide();$form.addClass('parcs-ht-gate-ready');function check(){var date=String(p.date.val()||'');if(!/^20\d{2}-\d{2}-\d{2}$/.test(date)){p.others.hide();showMessage($form,'','','');return;}$.post(config.ajaxUrl,{action:'parcs_ht_quote_date_status',nonce:config.nonce,date:date}).done(function(response){var data=response&&response.success?response.data:null;if(!data||!data.tariffs){p.others.hide();showMessage($form,config.unavailableEnabled?config.unavailableMessage:'',config.unavailableContact,'unavailable');return;}p.others.show();if(data.closed&&config.closedEnabled)showMessage($form,config.closedMessage,config.closedContact,'closed');else showMessage($form,'','','');p.date.trigger('parcsHtQuoteDateReady');}).fail(function(){p.others.hide();showMessage($form,config.unavailableEnabled?config.unavailableMessage:'Impossible de vérifier cette date pour le moment.',config.unavailableContact,'error');});}p.date.on('change',check);check();}
    function scan(){$('.parcs-ht-quote .wpcf7 form, .parcs-ht-quote form.wpcf7-form').each(function(){init($(this));});}
    $(scan);document.addEventListener('wpcf7init',scan);
})(jQuery);
